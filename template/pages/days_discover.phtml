<?
$this->title = "Interesting days";
$this->menu = 'interesting';
?>
{{wrap with="layout.phtml"}}
{{into slot="css"}}
	.carousel-caption a {
		color:#fff;
	}
	.carousel-caption a:hover {
		color:rgb(156, 0, 1);
		text-decoration:none;
	}
	.carousel-inner img {
		width:532px;
		height:532px;
	}
	.carousel-control.left, .carousel-control.right {
		font-size:80px;
	}
	.carousel-control.left {
		left:-55px;
	}
	.carousel-control.right {
		right:-55px;
	}
{{/into}}
{{into slot="content"}}
<!-- Main Area -->
<div id="main_area" style="padding-top:5px">
    <!-- Slider -->
    <div class="row">
        <div class="span6 offset3" id="slider">
            <div class="row">
                <div class="span6" id="carousel-bounding-box" style="width:532px">
                    <div id="myCarousel" class="carousel slide">
                        <div class="carousel-inner">
                            {{list using="$#days" key="{$i}"}}
		                            {{list:item}}
			                              <? $class = (!$i) ? 'active' : '';?>
				                            <div class="item {$class}" data-slide-number="{$i}">
						                            <a href="/pages/{$item.id}/day">
						                                <img src="{$item.image_532}"/>
                                        </a>
						                            <div class="carousel-caption">
                                            <a href="/pages/{$item.id}/day">
								                                {$item.title}
	                                          </a>
								                            <br/>
								                            <small>
                                                <a href="/pages/{$item.user.id}/user">{$item.user.name}</a>
								                            </small>
						                            </div>
				                            </div>
		                            {{/list:item}}
                            {{/list}}
                        </div>
                        <!-- Carousel nav -->
                        <a class="carousel-control left" href="#myCarousel" data-slide="prev">‹</a>
                        <a class="carousel-control right" href="#myCarousel" data-slide="next">›</a>
                    </div>
                </div>
            </div>

        </div>
    </div>
    <!--/Slider-->

    <div class="row hidden-phone" id="slider-thumbs">
        <div class="span12">
            <!-- Bottom switcher of slider -->
            <ul class="unstyled">
                {{list using="$#days" key="{$i}"}}
                {{list:item}}
                <li class="pull-left">
                    <a class="thumb" slider_pos="{$i}">
                        <img src="{$item.image_266}" style="width:53px;height:53px;padding-right:5px"/>
                    </a>
                </li>
                {{/list:item}}
                {{/list}}
            </ul>
        </div>
    </div>
</div>
{{/into}}
{{into slot="js_ready"}}
		$('#myCarousel').carousel({ interval:5000 });

		$('.thumb').click(function () {
				var id = parseInt($(this).attr("slider_pos"));
				$('#myCarousel').carousel(id);
		});
{{/into}}
{{/wrap}}
