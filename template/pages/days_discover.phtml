<?
$this->title = "Interesting days";
$this->menu = 'interesting';
?>
{{wrap with="layout.phtml"}}
{{into slot="content"}}
	{{list using="$#days"}}
		<ul class="thumbnails">
				{{list:item}}
		    <li class="span3">
				    <a href="/pages/{$item->id}/day">
			        <div class="thumbnail">
					        <?php $image = $item->image_532 ? $item->image_532 : ($item->moments) ? $item->moments[0]->image_532 : '/img/no_photo.png'; ?>
			            <img src="{$image}" alt="{$item->final_description}">
			            <h3>{$item->title}</h3>
			            <p><small>{$item->final_description}</small></p>
			        </div>
            </a>
		    </li>
				{{/list:item}}
		</ul>
	{{/list}}
{{/into}}
{{/wrap}}
